seed: 1
loader:
  batch_size: 16
  pin_memory: false
  num_workers: 16
logger:
  name: null
  version: null
  save_dir: ~/logs/intphys/debug
checkpoint:
  save_top_k: 1
  save_last: true
  verbose: true
  monitor: val_accuracy
  mode: max
  prefix: ''
  filepath: null
checkpoint_path: null
model:
  model: LSTMCNNBaseline
  multiscale: true
  location_embeddings: true
  text_encoder:
    name: LSTMEncoder
    hidden: 256
    bidirectional: false
    embedding_dim: 300
    glove: true
    dropout: 0.2
  image_encoder:
    name: resnet18
    num_layers: 4
    pretrained: true
    freeze: true
  mask_predictor:
    num_layers: 3
    kernel_size: 5
    stride: 2
    padding: 2
dataset:
  foo: bar
optimizer:
  foo: bar
scheduler:
  foo: bar
trainer:
  gpus: 1
  max_epochs: 30
  gradient_clip_val: 1.0
  val_check_interval: 1.0
  resume_from_checkpoint: null
